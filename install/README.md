# 🚀 آموزش نصب و راه‌اندازی Wyron Panel

این راهنما برای نصب پنل Wyron روی سیستم‌عامل‌های زیر تهیه شده است:

- Ubuntu 20.04 (Focal)
- Ubuntu 22.04 (Jammy)

---

## 📋 پیش‌نیازها

- سرور Ubuntu 20.04 یا 22.04
- دسترسی root یا sudo
- فایل باینری دریافت‌شده از پشتیبانی
- (اختیاری) دامنه برای فعال‌سازی SSL

---

# 1️⃣ ساخت پوشه نصب

```bash
sudo mkdir -p /opt/wyron
cd /opt/wyron
```

فایل باینری دریافتی از پشتیبانی را داخل این پوشه قرار دهید.

اگر نام فایل `wyron` است:

```bash
chmod +x wyron
```

(اختیاری) برای اجرای سراسری:

```bash
sudo ln -s /opt/wyron/wyron /usr/local/bin/wyron
```

---

# 2️⃣ نصب MongoDB 7

## نصب پیش‌نیازها

```bash
sudo apt update
sudo apt install -y gnupg curl
```

## افزودن کلید و ریپازیتوری

### Ubuntu 22.04

```bash
curl -fsSL https://pgp.mongodb.com/server-8.0.asc | \
sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg --dearmor

echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/8.0 multiverse" | \
sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
```

### Ubuntu 20.04

```bash
curl -fsSL https://pgp.mongodb.com/server-8.0.asc | \
sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg --dearmor

echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/8.0 multiverse" | \
sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
```

## نصب و فعال‌سازی

```bash
sudo apt update
sudo apt install -y mongodb-org
sudo systemctl enable mongod
sudo systemctl start mongod
```

---

# 3️⃣ ساخت کاربر دیتابیس

```bash
mongosh
```

```javascript
use admin

db.createUser({
  user: "root",
  pwd: "رمز_قوی_اینجا_قرار_دهید",
  roles: [ { role: "root", db: "admin" } ]
})

exit
```

---

# 4️⃣ فعال‌سازی احراز هویت MongoDB

```bash
sudo nano /etc/mongod.conf
```

اطمینان حاصل کنید این بخش فعال است:

```yaml
security:
  authorization: enabled

net:
  port: 27017
  bindIp: 127.0.0.1
```

سپس:

```bash
sudo systemctl restart mongod
```

---

# 🔑 فعال‌سازی لایسنس

1️⃣ از طریق تلگرام به پشتیبانی تیکت ارسال کنید. 
https://t.me/wyronapp
2️⃣ فایل مخصوص سرور شما ارسال می‌شود.

برای اولین بار اجرا کنید:

```bash
wyron start
```

در اجرای اول، یک **Fingerprint** نمایش داده می‌شود.

🔹 آن را برای پشتیبانی ارسال کنید.  
🔹 فایل `license.key` مخصوص سرور شما صادر می‌شود.

فایل `license.key` را داخل پوشه `/opt/wyron` قرار دهید.

---

# ⚙️ تولید و ویرایش کانفیگ

در اولین اجرا، فایل `config.yml` به صورت خودکار ساخته می‌شود.

پنل را متوقف کنید:

```bash
wyron stop
```

ویرایش فایل:

```bash
nano /opt/wyron/config.yml
```

---

# 📝 توضیح تنظیمات مهم

## 🔹 log
سطح لاگ سیستم  
پیشنهادی: `info`

---

## 🔹 mongodb.uri

فرمت اتصال:

```
mongodb://USERNAME:PASSWORD@HOST:PORT/admin
```

مثال:

```
mongodb://root:YourPassword@localhost:27017/admin
```

⚠️ اگر پورت MongoDB تغییر کرده، همان پورت را وارد کنید.

---

## 🔹 database_name
نام دیتابیس مورد استفاده پنل.

---

## 🔹 port
پورتی که پنل روی آن اجرا می‌شود (پیش‌فرض 8000).

---

## 🔹 allowed-hosts
دامنه‌هایی که اجازه دسترسی دارند.

---

## 🔹 allowed-origins
برای جلوگیری از خطای CORS استفاده می‌شود.  
باید شامل http و https دامنه شما باشد.

---

## 🔹 main-address
آدرس اصلی پنل (مثلاً https://yourdomain.com)

---

## 🔹 listen-on
پیشنهادی برای امنیت:

```
127.0.0.1
```

---

# 🔐 تنظیمات حساس (مهم)

مقادیر زیر به صورت خودکار در اولین اجرا تولید می‌شوند:

- jwt-secret
- internal-secret

⚠️ این مقادیر را تغییر ندهید مگر در صورت اطلاع کامل از عملکرد سیستم.  
تغییر اشتباه آن‌ها ممکن است باعث اختلال در احراز هویت یا از کار افتادن سیستم شود.

---

# 🔒 TLS (اختیاری)

اگر می‌خواهید پنل خودش SSL را مدیریت کند:

```
tls:
  enabled: true
```

در غیر این صورت (در صورت استفاده از Nginx یا Cloudflare)  
می‌توانید آن را false نگه دارید.

---

# 🚀 اجرای نهایی

بعد از اعمال تغییرات:

```bash
wyron start
```

---

# ✅ نکات مهم

- فایل `license.key` باید در پوشه نصب قرار داشته باشد.
- بعد از هر تغییر در کانفیگ، پنل را ریستارت کنید.
- پورت پنل را در فایروال مدیریت کنید.
- مقادیر امنیتی خودکار تولید شده را تغییر ندهید.

---

در صورت بروز هرگونه مشکل، با پشتیبانی در ارتباط باشید.
